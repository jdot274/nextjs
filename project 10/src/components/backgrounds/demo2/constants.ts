export const PULSE_ANIMATION_CONFIG = {
  duration: 2, // seconds
  sidebarDelay: 0.2, // seconds between sidebar items
  contentDelay: 0.15, // seconds between content items
  contentWidthRange: {
    min: 60,
    max: 100
  }
} as const;